spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    username: test
    password: test
    url: ${DATABASE_TEST_URL}

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}